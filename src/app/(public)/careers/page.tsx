import type { Metadata } from "next";

import { Page } from "@/components/shell";

export const metadata: Metadata = {
  title: "Careers",
};

export default function People() {
  return <Page>{/* <CreateResearchArea /> */} Careers</Page>;
}
